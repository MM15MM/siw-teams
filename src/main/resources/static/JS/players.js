
    function redirectToPlayerPage(playerId) {
        window.location.href = '/player/' + playerId;
    }
function redirectToPlayerPageAdmin(playerId) {
        window.location.href = '/admin/player/' +  playerId;
    }